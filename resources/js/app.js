Ext.onReady(function() {
    Ext.create('Ext.Panel', {
        renderTo: 'grid',
        height: 200,
        width: 400,
        title: 'Motocard',
        html: 'Replace this panel with a grid panel'
    });
});
